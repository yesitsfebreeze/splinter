[package]
name = "split-language-py"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
split-language-common = { path = "../common" }

[profile.release]
opt-level = "s"
strip = true
