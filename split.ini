# split configuration
# Safe to commit — no secrets, project-level defaults only.
# Env vars take precedence over values here.

SPLIT_EXT         = rs
SPLIT_SRC_DIR     = src
SPLIT_DEBOUNCE_MS = 500
SPLIT_MAX_LOC     = 256
